// Person Service
class PersonService {
  constructor() {
    // TODO: Implement person service
  }

  // Service methods:
  // - createPerson(personData, userId)
  // - getPerson(id)
  // - searchPersons(query, filters)
  // - updatePerson(id, data, userId)
  // - deletePerson(id)
  // - mergePersonRecords(primaryId, secondaryId)
  // - addAssociation(personId, associationType, associatedPersonId)
  // - getRelatedInvestigations(personId)
}

module.exports = PersonService;
